# H2 in-memory DB for tests
DB_CLOSE_DELAY=-1
spring.test.database.replace=none

db.driver=org.h2.Driver
db.url=jdbc:h2:mem:blogdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;DB_CLOSE_DELAY=-1
db.username=sa
db.password=

